reverseproxy:
  default_backend: "secondary"
  backend_services:
    secondary: "http://localhost:9002"
  feature_flags:
    flags:
      beta-features: false
      legacy-mode: true
      stable-features: true