// Package tenant provides tenant guard, isolation and enforcement logic.
// RED phase tests are hidden behind the 'failing_test' build tag until the
// minimal implementation is merged.
package tenant
